# Landing-test

Repositorio inicializado con una estrategia de ramas basada en `main`, `dev` y ramas `feature/*`.

- `main`: rama principal y estable.
- `dev`: rama de integración para desarrollo.
- `feature/*`: ramas destinadas a cada cambio o funcionalidad.

Los cambios se incorporan mediante pull requests.
